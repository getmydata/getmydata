<% if @companies[0].category == "Empty" %>
  <%= render "no-companies-to-show" %>
<% else %>
<% @companies.sort_by {|company| company.name.downcase}.each do |company| %>
    <%= render "select-company", company: company %>
  <% end%>
<% end%>
