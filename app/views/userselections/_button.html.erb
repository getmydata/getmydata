 <li id="selected-list"><%= selection.company.name.sub(/^./, &:upcase) %>
  <%= simple_form_for(user_selection) do |f| %>
    <%= link_to destroy_user_selections_path(selection.id), :method => :delete do %>
    <i class="fa fa-times-circle"></i>
    <% end %>
  <% end %>
</li>
